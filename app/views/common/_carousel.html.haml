.row.carousel
	.chevron.span.buttons.previous
		%a{:class => 'prev'}
			%i{:class => "fa fa-chevron-left fa-5x"}
	.viewport.span8
		%ul.overview
			- tiles.each do |tile|
				= render "common/tile", { tile: tile, link: link }
	.span.chevron.buttons.next
		%a{:class => 'next'}
			%i{:class => "fa fa-chevron-right fa-5x"}
